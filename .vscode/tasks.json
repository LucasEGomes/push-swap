{
    "tasks": [
        {
            "type": "shell",
            "label": "Build push swap program",
            "command": "/usr/bin/make",
            "args": [
                "re"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        }
    ],
    "version": "2.0.0"
}